Install ffmpeg with this commands

sudo apt-get update
sudo apt-get install ffmpeg -y

---------------------------------------

install pip with this command

sudo apt-get install python3-pip -y

---------------------------------------

install docker with this command

sudo apt install docker.io -y

----------------------------------------

install required libraries with this command

pip3 install -r requirements.txt

---------------------------------------

sudo docker build -t my_bot_app .

sudo docker run my_bot_app

----------------------------------------

apt update -y && apt-get update -y && apt-get install ffmpeg -y && apt install python3-pip -y && apt install docker.io -y && git clone https://github.com/aoisora999/ymca_bot.git mirrorbot/ && cd mirrorbot && pip3 install -r requirements.txt --break-system-packages && sudo docker build -t my_bot_app . && sudo docker run my_bot_app

